<%= simple_form_for(@list) do |f| %>
<%= f.input :name%>
<%= f.input :photo, as: :file %>
<%= f.button :submit, "Add List", class: "btn btn-primary rounded-pill" %>
<%= link_to "Cancel", request.referer || lists_path, class: "btn btn-secondary" %>
<% end %>
